# TP-002-agent-run-contract: TP-002-agent-run-contract — Status

**Current Step:** Complete
**Status:** ✅ Complete
**Last Updated:** 2026-07-23
**Review Level:** 2
**Review Counter:** 2
**Iteration:** 1
**Size:** L

---

### Step 0: Preflight

**Status:** ✅ Complete

- [x] Worktree has gitignored files copied from main checkout: `src/lib/generated/bindings.ts`, `src-tauri/excalidraw-dist/` (or regenerate bindings: `cargo test --manifest-path src-tauri/Cargo.toml specta_export`)
- [x] Baseline gates green before edits (nohup + poll for long runs — this host kills Bash at 120s): `nohup pnpm test > .tmpfiles/tp002-base-vitest.log 2>&1 &` and `nohup cargo test --manifest-path src-tauri/Cargo.toml > .tmpfiles/tp002-base-cargo.log 2>&1 &`

---

### Step 1: Rust unified command + registry

**Status:** ✅ Complete

- [x] Define `AgentRunBackend` enum (`"harness" | "native"`, snake_case serde) and `AgentRunSpec` struct: `{ provider_config, prompt, vault_path, permission_mode, resume_session_id?, backend }` — specta-typed
- [x] New commands `agent_run_start(request_id, spec)` / `agent_run_abort(request_id)`; single channel `agent-run-event:{request_id}`; ONE registry struct replacing `AgentStreamState` + `NativeAgentState`; route to the EXISTING harness spawn logic and `run_native_turn` unchanged
- [x] `AgentEvent`: add `#[serde(rename = "reasoning")] Reasoning { delta: String }` and `result_summary: Option<String>` on `ToolEnd` (`#[serde(default, skip_serializing_if = "Option::is_none")]`). NO other variant changes — existing variants byte-stable
- [x] Native loop: map `AiStreamEvent::Reasoning { text }` → `AgentEvent::Reasoning { delta: text }`; populate `result_summary: None` everywhere for now
- [x] Delete old commands/registries/channels (clean break — repo rule: internal API breaks OK, 0 users)
- [x] `cargo test specta_export` to regenerate bindings.ts

---

### Step 2: TS single adapter + port

**Status:** ✅ Complete

- [x] `agent_events.ts`: add `{ type: "reasoning"; delta: string }`; `result_summary?: string | null` on tool_end
- [x] `AgentStreamRequest` gains `backend: "harness" | "native"`
- [x] Rewrite `agent_tauri_adapter.ts` as THE adapter (channel `agent-run-event:`), delete `native_agent_tauri_adapter.ts`
- [x] `rag_actions.ts`: single `agent_port` + single `AgentRunner`; `agent_backend(provider)` result passed as `request.backend` (keep `agent_backend()` as-is — TP-004 replaces it)
- [x] Update affected unit tests

---

### Step 3: Testing & Verification

**Status:** ✅ Complete

- [x] Targeted: `cargo test --manifest-path src-tauri/Cargo.toml agent` and `pnpm vitest run tests/unit/services/agent_runner.test.ts` (and adapter tests)
- [x] `grep -rn "native-agent-stream-event\|native_agent_stream_start" src/ src-tauri/src/` → ZERO hits outside history/comments
- [x] FULL gates (nohup pattern): `pnpm check` 0 errors, `pnpm lint` (layering pass), `pnpm test` all green, `cargo test` all green, then `pnpm format`
- [x] Fix all failures

---

### Step 4: Documentation & Delivery

**Status:** ✅ Complete

- [x] Discoveries logged in STATUS.md (esp. any place that hardcoded the old channel names)
- [x] One command pair / one channel / one registry / one adapter; old ones gone
- [x] AgentEvent extension additive; existing consumers render unchanged
- [x] All gates green; `pnpm format` applied

---

## Reviews

| #   | Type | Step | Verdict | File |
| --- | ---- | ---- | ------- | ---- |

---

## Discoveries

| Discovery | Disposition | Location |
| --------- | ----------- | -------- |
| AgentRunner does not handle `reasoning` events — silently ignored in the event loop. Additive extension is safe; wiring reasoning display is TP-003/TP-004 scope. | Logged as tech debt | `src/lib/features/rag/application/agent_runner.ts` |
| `archive/` directory was not in `.oxlintrc.json` ignorePatterns — added `archive/**` to prevent lint errors on archived files. | Fixed | `.oxlintrc.json` |
| Pre-existing lint errors in test files (require-await, no-confusing-void-expression, etc.) — not introduced by this task. | Noted | Various test files |

---

## Execution Log

| Timestamp        | Action         | Outcome                                         |
| ---------------- | -------------- | ----------------------------------------------- |
| 2026-07-23       | Task staged    | STATUS.md auto-generated by task-runner         |
| 2026-07-23 18:45 | Task started   | Runtime V2 lane-runner execution                |
| 2026-07-23 18:45 | Step 0 started | Preflight                                       |
| 2026-07-23 18:45 | Worker iter 1  | error (code 1) in 1s, tools: 0                  |
| 2026-07-23 18:45 | No progress    | Iteration 1: 0 new checkboxes (1/3 stall limit) |
| 2026-07-23 18:45 | Worker iter 2  | error (code 1) in 1s, tools: 0                  |
| 2026-07-23 18:45 | No progress    | Iteration 2: 0 new checkboxes (2/3 stall limit) |
| 2026-07-23 18:45 | Worker iter 3  | error (code 1) in 1s, tools: 0                  |
| 2026-07-23 18:45 | No progress    | Iteration 3: 0 new checkboxes (3/3 stall limit) |
| 2026-07-23 18:45 | Task blocked   | No progress after 3 iterations                  |
| 2026-07-23 19:12 | Task started   | Runtime V2 lane-runner execution                |
| 2026-07-23 19:33 | Worker iter 1 | done in 1222s, tools: 186 |
| 2026-07-23 19:33 | Task complete | .DONE created |

---

## Blockers

_None_

---

## Notes

_Reserved for execution notes_
