---
"carbide": patch
---

Table toolbar no longer vanishes after clicking an option (the transient focusout from ProseMirror rebuilding cell/table DOM is now ignored while focus stays in the editor), and terminal Option+Arrow again jumps by word (mapped to meta-b/meta-f, with macOptionIsMeta restored).
