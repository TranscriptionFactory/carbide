---
"carbide": minor
---

Restore reference backend (linked sources, Zotero BBT, annotations); fix window close permissions, trailing whitespace in paragraphs, find-replace after buffer switch, virtualizer row height re-measurement, and iCloud settings write.
