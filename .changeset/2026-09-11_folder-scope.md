---
"carbide": minor
---

The omnibar and search graph gain a first-class folder scope that narrows results to a folder and its descendants, replacing the omnibar's imprecise path-string seed. Folder scope composes with text and structured queries and stays current-vault only.
