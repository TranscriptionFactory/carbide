---
"carbide": patch
---

Quitting via the tray menu or Cmd+Q now prompts to save unsaved changes, matching the window close button. The post-update restart toast persists until dismissed instead of expiring after 30 seconds.
