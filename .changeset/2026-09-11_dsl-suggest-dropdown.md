---
"carbide": patch
---

The DSL suggest dropdown scrolls the selected row into view when selection moves past the visible window, and hovering a row updates the selected index so hover-then-Enter commits the hovered row.
