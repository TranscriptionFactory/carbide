---
"carbide": patch
---

Closing a search graph tab now frees its results however the tab was closed. Before, only the tab's own close button released the snapshot. Closing from the tab bar, with a shortcut, or with close others/right/all left every search's nodes, edges and snippets in memory until the app restarted.
