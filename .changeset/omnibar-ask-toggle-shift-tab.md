---
"carbide": patch
---

Omnibar Search/Ask mode toggle moved from ⌘/ to Shift+Tab: the global editor-mode hotkey (CmdOrCtrl+/, capture phase) always won the chord, so the palette toggle flipped the note's source/visual mode instead. Clearing all filters, previously on Shift+Tab, is now the X mnemonic inside the Tab filter layer.
