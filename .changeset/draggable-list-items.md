---
"carbide": minor
---

Individual list items (including tasks) now have their own drag handles at every nesting depth and can be dragged and dropped to reorder within or across lists using ProseMirror's schema-aware placement, instead of the whole list moving as one block.
