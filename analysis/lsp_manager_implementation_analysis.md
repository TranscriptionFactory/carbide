[{'vault_clients': 'Mutex<HashMap<String', 'config': 'LspManagerConfig', 'client_usage': 'Mutex<HashMap<String', 'std': 'time::Instant>>'}, ['derive(Debug, Clone)]\npub struct LspManagerConfig {\n pub languages: HashMap<String, LanguageLspConfig>,\n pub max_restarts: u32,\n pub backoff_ms: Vec<u64>,\n}\n\n#[derive(Debug, Clone)]\npub struct LanguageLspConfig {\n pub binary: String,\n pub args: Vec<String>,\n pub capabilities: serde_json::Value,\n pub enabled: bool,\n}\n\nimpl LspManager {\n pub async fn ensure_language_lsp(\n &mut self,\n vault_id: &str,\n language_id: &str,\n ) -> Result<&RestartableLspClient, String> {\n // Implementation to start LSP client if not already running\n }\n}\n`\n\n**Language Detection**:\n`rust\nfn detect_language_from_extension(extension: &str) -> Option<&str> {\n match extension.to_lowercase().as_str() {\n "py" => Some("python', 'rs" => Some("rust', 'ts" | "tsx" => Some("typescript', 'js" | "jsx" => Some("javascript', 'md" => Some("markdown', 'None,\n }\n}\n`\n\n## 2. Resource Management for Multiple LSP Clients\n\n### Current Resource Management\nThe existing IWE implementation demonstrates good resource management practices:\n- **Process lifecycle**: Automatic restart with exponential backoff\n- **Memory cleanup**: Proper shutdown of LSP processes\n- **Notification handling**: Efficient forwarding of LSP notifications\n\n### Resource Management Challenges for PDF Omnifind\n1. **Process proliferation**: Multiple LSP clients per vault could lead to many processes\n2. **Memory consumption**: Each LSP client maintains its own state and buffers\n3. **Lifecycle coordination**: Starting/stopping multiple LSP clients in sync\n\n### Proposed Resource Management Solutions\n`rust\n// Resource limits\nconst MAX_LSP_CLIENTS_PER_VAULT: usize = 5;\n\nimpl LspManager {\n fn can_start_new_client(&self, vault_id: &str) -> bool {\n let client_count = self\n .vault_clients\n .lock()\n .await\n .get(vault_id)\n .map(|clients| clients.len())\n .unwrap_or(0);\n client_count < MAX_LSP_CLIENTS_PER_VAULT\n }\n \n // Client eviction based on usage patterns\n async fn evict_least_used_client(&self, vault_id: &str) -> Option<String> {\n // Implementation to remove least recently used LSP client\n }\n}\n``\n\n**Resource Management Strategies**:\n1. **Lazy initialization**: Only start LSP clients when files of that type are opened\n2. **Resource pooling**: Reuse LSP clients for similar file types\n3. **Memory limits**: Implement client eviction based on usage patterns\n4. **Graceful degradation**: Prioritize critical LSP clients when resources are constrained\n\n## 3. Error Handling and Graceful Degradation\n\n### Current Error Handling\nThe existing implementation provides robust error handling:\n- **Restart logic**: Automatic restart with exponential backoff (3 attempts by default)\n- **Status reporting**: LSP session status changes are reported via channels\n- **Error propagation**: `LspClientError` enum covers various failure modes:\n  - `ProcessSpawnFailed`\n  - `ProcessExited`\n  - `RequestTimeout`\n  - `InvalidResponse`\n  - `ShutdownFailed`\n  - `ChannelClosed`\n\n### Error Handling Gaps for Multi-Language Support\n1. **Partial LSP failure**: When one LSP fails, others should continue working\n2. **Fallback strategies**: No fallback when preferred LSP is unavailable\n3. **User feedback**: Limited error reporting to users\n\n### Proposed Error Handling Improvements\n``rust\nimpl LspManager {\n pub async fn send_request_to_language(\n &self,\n vault_id: &str,\n language_id: &str,\n method: &str,\n params: serde_json::Value,\n ) -> Result<serde_json::Value, String> {\n let clients = self.vault_clients.lock().await;\n let client = clients\n .get(vault_id)\n .and_then(|langs| langs.get(language_id))\n .ok_or_else(|| format!("LSP not started for {} in vault {}', 'language_id, vault_id))?;\n \n client.send_request(method, params).await\n .map_err(|e| format!("LSP request failed for {}: {}', 'language_id, e))\n }\n \n pub fn get_available_languages(&self, vault_id: &str) -> Vec<String> {\n self.vault_clients\n .lock()\n .await\n .get(vault_id)\n .map(|clients| clients.keys().cloned().collect())\n .unwrap_or_default()\n }\n}\n``\n\n**Enhanced Error Handling**:\n1. **Isolated failures**: Ensure one LSP failure doesn\'t affect others\n2. **Graceful degradation**: Continue with reduced functionality when LSPs fail\n3. **Enhanced error reporting**: Provide detailed error information to users\n4. **Fallback mechanisms**: Use basic syntax highlighting when LSP is unavailable\n\n## 4. Configuration Management and Extensibility\n\n### Current Configuration System\nThe existing IWE feature uses a straightforward configuration approach:\n- `LspClientConfig` struct for binary path, args, root URI, capabilities\n- Tauri commands exposed via specta for TypeScript binding generation\n- Toolchain resolver for dynamic binary resolution\n\n### Configuration Requirements for PDF Omnifind\n1. **Language-specific settings**: Different LSPs need different configurations\n2. **User customization**: Allow users to configure LSP settings\n3. **Vault-specific settings**: Different vaults may need different configurations\n\n### Proposed Configuration System\n``rust\n#[derive(Debug, Clone)]\npub struct LspManagerConfig {\n pub languages: HashMap<String, LanguageLspConfig>,\n pub max_restarts: u32,\n pub backoff_ms: Vec<u64>,\n}\n\n#[derive(Debug, Clone)]\npub struct LanguageLspConfig {\n pub binary: String,\n pub args: Vec<String>,\n pub capabilities: serde_json::Value,\n pub enabled: bool,\n}\n\nimpl LspManagerConfig {\n pub fn default() -> Self {\n Self {\n languages: {\n let mut map = HashMap::new();\n map.insert("python', '.', 'to_string(), LanguageLspConfig {\n binary: "pyright', '.', 'to_string(),\n args: vec!["--stdio', '.', 'to_string()],\n capabilities: serde_json::json!({\n "textDocument": {\n "synchronization": { "didSave', True], {'relatedInformation': True}, {'binary': 'rust-analyzer', 'args': 'vec![]', 'capabilities': 'serde_json::json!({', 'textDocument': {'synchronization': {'didSave': True}, 'publishDiagnostics': {'relatedInformation': True}}}, [1000, 2000, 4000], {'vault_id': 'str', 'language_id': 'str', 'log': 'info!(', 'language': {}, 'vault': {}, 'format!(': 'o configuration for language: {}', 'Err(format!(': 'anguage {} is disabled', 'client': {}, 'room': 'evicted);'}, {'reached': 'to_string());'}, {}, {'binary_path': 'config.binary.clone()', 'args': 'config.args.clone()', 'capabilities': 'config.capabilities.clone()', 'working_dir': 'Some(vault_path.to_string_lossy().into_owned())'}, {'max_restarts': 'self.config.max_restarts', 'backoff_ms': 'self.config.backoff_ms.clone()'}, {'tokio': 'spawn(forward_notifications(\n notification_rx', 'or_insert_with(HashMap': 'new)\n .insert(language_id.to_string()', 'log': 'info!(', 'language': {}, 'vault': {}, 'tauri': 'command]\n#[specta::specta]\npub async fn lsp_send_request(\n app: AppHandle', 'vault_id': 'str', 'language_id': 'String', 'method': 'String', 'params': 'serde_json::Value', 'Result<serde_json': 'Value', 'integrations': 'rust\nimpl LspManager {\n // Maintain compatibility with existing single-client pattern\n pub async fn get_or_start_default_client(\n &mut self', 'markdown").await?;\n Ok(self.get_client(vault_id, "markdown': ''}, [], {'mount_multi_language_effects(editor_store': "EditorStore): void {\n $effect(() => {\n const open_note = editor_store.open_note;\n if (!open_note) return;\n \n const file_type = detect_file_type(open_note.meta.path);\n const language_id = get_lsp_language(file_type);\n \n if (language_id) {\n // Ensure LSP client for this language is started\n void invoke('lsp_start_language'", 'vaultId': 'editor_store.vault_id', 'languageId': 'language_id'}]
