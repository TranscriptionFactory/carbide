[{'vault_clients': 'HashMap<String', 'language_configs': 'HashMap<String', 'vault_id': 'str) -> bool {\n let client_count = self\n .vault_clients\n .get(vault_id)\n .map(|clients| clients.len())\n .unwrap_or(0);\n client_count < MAX_LSP_CLIENTS_PER_VAULT', 'language_id': 'str', 'mapping': 'rust\nfn detect_language_from_extension(extension: &str) -> Option<&str> {\n match extension.to_lowercase().as_str() {', 'rs" => Some("rust"),\n "ts" | "tsx" => Some("typescript"),\n "js" | "jsx" => Some("javascript"),\n "md" => Some("markdown': 'None', 'service': 'HashMap<String', 'needed': 'rust\nimpl LspManager {\n pub async fn ensure_language_lsp(\n &mut self', 'limits': 'rust\nconst MAX_LSP_CLIENTS_PER_VAULT: usize = 5;\n\nimpl LspManager {\n fn can_start_new_client(&self'}, {'vault_id': 'String', 'language_id': 'String', 'last_used': 'std::time::Instant'}, {'others': 'rust\nimpl LspManager {\n pub async fn send_request_to_language(\n &self', 'vault_id': 'str) -> Vec<String> {\n self.vault_clients\n .get(vault_id)\n .map(|clients| clients.keys().cloned().collect())\n .unwrap_or_default()', 'language_id': 'str', 'method': 'str', 'params': 'serde_json::Value', 'Result<serde_json': 'Value', 'format!(': 'SP request failed for {}: {}', 'fail': 'rust\nimpl LspManager {\n pub fn get_available_languages(&self'}, {'language': 'rust\n#[derive(Debug', 'binary': 'String', 'args': 'Vec<String>', 'capabilities': 'serde_json::Value', 'enabled': 'bool'}, {'binary': 'auto', 'args': 'vec![]', 'capabilities': 'serde_json::json!({'}, {'languages': 'HashMap<String', 'max_restarts': 'u32', 'backoff_ms': 'Vec<u64>'}, {'new_config': 'LspManagerConfig) -> Result<()', 'language_id': 'str) -> Option<&LanguageLspConfig> {\n self.config.languages.get(language_id)'}, ['tauri::command]\n#[specta::specta]\npub async fn lsp_start_language(\n app: AppHandle,\n vault_id: String,\n language_id: String,\n) -> Result<(), String> {\n let state = lint_state(&app);\n let manager = state.lsp_manager.lock().await;\n manager.start_language_lsp(&vault_id, &language_id).await\n}\n\n#[tauri::command]\n#[specta::specta]\npub async fn lsp_send_request(\n app: AppHandle,\n vault_id: String,\n language_id: String,\n method: String,\n params: serde_json::Value,\n) -> Result<serde_json::Value, String> {\n let state = lint_state(&app);\n let manager = state.lsp_manager.lock().await;\n manager.send_request_to_language(&vault_id, &language_id, &method, params).await\n}\n`\n\n#### Backward Compatibility\nMaintain existing IWE and lint LSP integrations:\n\n`rust\nimpl LspManager {\n // Maintain compatibility with existing single-client pattern\n pub async fn get_or_start_default_client(\n &mut self,\n vault_id: &str,\n ) -> Result<&RestartableLspClient, String> {\n // For backward compatibility, default to markdown LSP\n self.ensure_language_lsp(vault_id, "markdown', '.', 'await?;\n Ok(self.get_client(vault_id, "markdown', '.', "unwrap())\n }\n}\n`\n\n#### Frontend Integration\nExtend frontend reactors to work with multiple LSP clients:\n\n`typescript\n// In src/lib/reactors/lsp_document_sync.reactor.svelte.ts\nexport function create_lsp_document_sync_reactor(\n editor_store: EditorStore,\n clients: LspSyncClientConfig[],\n): () => void {\n return $effect.root(() => {\n // Mount reactors for each LSP client\n for (const client of clients) {\n mount_client_effects(editor_store, client);\n }\n \n // Add new reactor for multi-language LSP support\n mount_multi_language_effects(editor_store);\n });\n}\n\nfunction mount_multi_language_effects(editor_store: EditorStore): void {\n $effect(() => {\n const open_note = editor_store.open_note;\n if (!open_note) return;\n \n const file_type = detect_file_type(open_note.meta.path);\n const language_id = get_lsp_language(file_type);\n \n if (language_id) {\n // Ensure LSP client for this language is started\n void invoke('lsp_start_language', {\n vaultId: editor_store.vault_id,\n languageId: language_id\n });\n }\n });\n}\n```\n\n## Implementation Steps\n\n### Phase 1: Core LSP Manager (Days 1-3)\n1. Create `LspManager`service in`src-tauri/src/features/lint/lsp_manager.rs`\n2. Implement multi-client management with HashMap storage\n3. Add language detection based on file extensions\n4. Create basic Tauri commands for LSP manager operations\n5. Implement lazy initialization of LSP clients\n\n### Phase 2: Configuration System (Days 4-5)\n1. Design configuration schema for multi-LSP settings\n2. Implement configuration loading and validation\n3. Add support for language-specific LSP configurations\n4. Create Tauri commands for configuration management\n5. Add validation for LSP binary paths and capabilities\n\n### Phase 3: Resource Management (Days 6-7)\n1. Implement resource limits for LSP clients\n2. Add client eviction based on usage patterns\n3. Optimize memory usage and process management\n4. Add monitoring and metrics for LSP operations\n5. Implement performance testing framework\n\n### Phase 4: Error Handling and Graceful Degradation (Days 8-9)\n1. Enhance error handling for isolated LSP failures\n2. Implement graceful degradation strategies\n3. Add comprehensive error reporting to frontend\n4. Create user feedback mechanisms for LSP status\n5. Implement fallback mechanisms for critical operations\n\n### Phase 5: Frontend Integration (Days 10-12)\n1. Extend document sync reactor for multi-language support\n2. Update diagnostics system to handle multiple LSP sources\n3. Implement language-specific UI features\n4. Add LSP status indicators and error reporting UI\n5. Create user interface for LSP configuration management\n\n## Testing Strategy\n\n### Unit Tests\n1. Language detection from file extensions\n2. LSP client lifecycle management\n3. Configuration validation and loading\n4. Error handling and graceful degradation\n5. Resource management and limits\n\n### Integration Tests\n1. Multi-LSP client startup and shutdown\n2. Document synchronization across multiple LSPs\n3. Diagnostics forwarding from multiple sources\n4. Configuration updates and dynamic reloading\n5. Performance under load with multiple LSP clients\n\n### Performance Benchmarks\n1. LSP client startup time\n2. Memory usage with multiple LSP clients\n3. Document synchronization latency\n4. Request/response performance\n5. Resource consumption under various loads\n\n## Risk Mitigation\n\n### High-Risk Items\n1. **Resource Exhaustion**: Implement hard limits and monitoring\n2. **Performance Degradation**: Add performance testing and optimization\n3. **Configuration Complexity**: Provide sensible defaults and simplified UI\n\n### Medium-Risk Items\n1. **Compatibility Issues**: Test with various LSP implementations\n2. **Error Propagation**: Isolate LSP clients to prevent cascading failures\n3. **User Confusion**: Provide clear status information and documentation\n\n### Low-Risk Items\n1. **Backward Compatibility**: Maintain existing APIs and behavior\n2. **Extensibility**: Design modular architecture for future enhancements\n3. **Maintainability**: Follow existing code patterns and conventions\n\n## Conclusion\n\nThe implementation of multi-language LSP support for PDF Omnifind is feasible with the existing infrastructure. The key is to build upon the proven patterns established by the IWE feature while addressing the specific requirements for managing multiple LSP clients efficiently. The phased approach ensures that each component is thoroughly tested and validated before moving to the next phase, reducing overall risk and ensuring a stable implementation."]]
